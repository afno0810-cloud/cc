{
  "name": "marinor-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:embed": "vite build --config vite.embed.config.js",
    "build:pages": "node scripts/build-preview.mjs .pages-tmp && rm -rf docs/assets docs/media docs/index.html && cp -r .pages-tmp/. docs/ && touch docs/.nojekyll && rm -rf .pages-tmp .pages-tmp-build"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.5.0",
    "vite": "^5.4.21"
  },
  "dependencies": {
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@fontsource/big-shoulders-display": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "lenis": "^1.1.13",
    "three": "^0.169.0",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  }
}
